version: "2"
run:
  timeout: 5m
  issues-exit-code: 1

linters:
  enable:
    - errcheck
    - govet
    - dupl
    - goconst
    - misspell
    - funlen
    - ineffassign
    - staticcheck

issues:
  max-same-issues: 0
