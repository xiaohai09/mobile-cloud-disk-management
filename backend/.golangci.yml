version: "2"
run:
  timeout: 5m
  issues-exit-code: 1

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - dupl
    - misspell
    - ineffassign
  disable:
    - staticcheck
    - unused
    - funlen
    - goconst
    - gocyclo

issues:
  max-same-issues: 0
